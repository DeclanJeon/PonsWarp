[workspace]
resolver = "2"
members = [
    "ponswarp-signaling-rs",
    "pons-core-wasm",
]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true

[profile.release.package."*"]
opt-level = 3

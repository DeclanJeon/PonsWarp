# PonsWarp QA Report — Security Hardening & 1:1 P2P

> 날짜: 2026-08-18 · 대상: master (`a3dbc34` + 작업 변경분)
> 범위: 보안 하드닝 (P0/P1) + 릴레이/스웜 UI 제거 (1:1 P2P) + QA 보고

---

## 1. 검증 요약

| 게이트 | 명령 | 결과 |
|---|---|---|
| ESLint | `eslint src --ext .ts,.tsx` | ✅ 0 errors / 12 warns (`no-explicit-any` 기존) |
| TypeScript | `tsc --noEmit` | ✅ 0 errors |
| Frontend 단위 | `vitest run` | ✅ 32 suites / 166 tests pass |
| Backend 단위 | `cargo test --locked` | ✅ 65 lib + 2 origin + 0 doc = 67 pass |
| 의존성 | `pnpm audit --prod` | ✅ 0 vulnerabilities |
| 프로덕션 빌드 | `vite build` | ✅ 2181 modules, 5.46s |
| WASM 증명 | `verify-wasm-provenance.mjs` | ✅ digest `c755a559…` |
| 백엔드 컴파일 | `cargo check --locked` | ✅ |
| compose | `docker compose config -q` | ✅ 유효 |

## 2. 검증 상세

### 2.1 테스트 수정 이력
- `RefreshTurnCredentials`에 `credential` 필드 추가 시 `protocol::contract::tests::v1_client_fixtures_round_trip_without_wire_drift` 실패
  → `#[serde(default, skip_serializing_if = "Option::is_none")]` 적용으로 기존 와이어 형식 유지, **65개 테스트 전부 통과**

### 2.2 이번 변경분 검증 초점
| 항목 | 검증 | 결과 |
|---|---|---|
| TURN HMAC 검증 (`validate_credentials(username, secret, credential)`) | 기존 TURN 발급 + Refresh 경로 컴파일/테스트 | ✅ |
| Billing return_url origin 검증 + PayPal token 캐시 | 빌드/테스트 | ✅ |
| 시그널링 입력 크기 제한 (SDP/ICE/Manifest) | 컴파일 | ✅ |
| Mesh memory 우회 차단 (`PONSWARP_MESH_ALLOW_INSECURE_MEMORY`) | 컴파일 | ✅ |
| 1:1 UI 전환 (Sender/Receiver/Store) | type-check + vitest + lint | ✅ |
| 프로토콜 컨트랙트 drift | contract test | ✅ (수정 후) |

## 3. 남은 이슈 (비차단)

- `swarmManager`/`webRTCService` 등에 `any` 12건 (warn, 기존 부채)
- `swarmManager` 내부 엔진 로직(`MAX_DIRECT_PEERS`, hybrid 암 등)은 이번 UI 정리 범위 밖 (후속 M5)
- WASM 크레이트는 `--offline` 의존성 부재로 단독 `cargo check` 미수행 (signaling 대상만 수행; 빌드 산출물은 provenance 통과)

## 4. 릴리스 판정

**PASS** — 전 게이트 green. 배포 진행 가능.

| 항목 | 값 |
|---|---|
| 릴리스 기준 | 작업 브랜치 master (커밋 예정 SHA) |
| 배포 방법 | `deploy/deploy-production.sh` (host-only secrets, blue/green 5502↔5503) |
| 배포 후 smoke | `/health` `/ready` `/` + WS upgrade 101 |
| 옵션 QA | `PONSWARP_RUN_PROD_TRANSFER_QA=1` 시 실전 전송 QA |

---

*작성: PonsWarp QA · 검증 명령은 `section 1` 표 참조*
